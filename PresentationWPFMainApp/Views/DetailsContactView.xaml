<UserControl x:Class="PresentationWPFMainApp.Views.DetailsContactView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             
             xmlns:local="clr-namespace:PresentationWPFMainApp.Views"
             xmlns:viewmodels="clr-namespace:PresentationWPFMainApp.ViewModels"
             xmlns:models="clr-namespace:Business.Models;assembly=Business"
             d:DataContext="{d:DesignInstance Type=viewmodels:DetailsContactViewModel}"
             
             Background="White"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="350">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Text="Contact Details" FontSize="20" FontWeight="Bold" />

        <StackPanel Grid.Row="1" Margin="0 20 0 0">
            <TextBlock Text="First name" />
            <TextBox Text="{Binding Contact.FirstName}" Margin="0 0 0 10" Padding="4" />

            <TextBlock Text="Last name" />
            <TextBox Text="{Binding Contact.LastName}" Margin="0 0 0 10" Padding="4" />

            <TextBlock Text="Email" />
            <TextBox Text="{Binding Contact.Email}" Margin="0 0 0 10" Padding="4" />

            <TextBlock Text="Phone" />
            <TextBox Text="{Binding Contact.Phone}" Margin="0 0 0 10" Padding="4" />

            <TextBlock Text="Street address" />
            <TextBox Text="{Binding Contact.StreetAddress}" Margin="0 0 0 10" Padding="4" />

            <TextBlock Text="Postal code" />
            <TextBox Text="{Binding Contact.PostalCode}" Margin="0 0 0 10" Padding="4" />

            <TextBlock Text="City" />
            <TextBox Text="{Binding Contact.City}" Margin="0 0 0 10" Padding="4" />
        </StackPanel>

        <StackPanel Grid.Row="2">
            <Button Content="Edit" Command="{Binding GoToEditContactCommand}" Margin="0 10 0 5" Padding="5" />
            <Button Content="Delete" Command="{Binding DeleteCommand}" Margin="0 10 0 5" Padding="5" />
            <Button Content="Cancel" Command="{Binding CancelCommand}" Margin="0 10 0 5" Padding="5" />
        </StackPanel>

    </Grid>
</UserControl>
